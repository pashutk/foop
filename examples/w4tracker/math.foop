import True, False from ./Bool

wasm add(a, b) {
  (i32.add (local.get $a) (local.get $b))
}

wasm eqNumeric(a, b) {
  (i32.eq (local.get $a) (local.get $b))
}

function eq(a, b) {
  match(eqNumeric(a, b)) {
    1 => True
    otherwise => False
  }
}
